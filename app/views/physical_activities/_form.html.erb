<%= form_for(@physical_activity) do |f| %>
  <% if @physical_activity.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@physical_activity.errors.count, "error") %> prohibited this physical_activity from being saved:</h2>

      <ul>
      <% @physical_activity.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
